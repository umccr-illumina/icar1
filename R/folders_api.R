#' Workflow Execution Service
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Folders operations
#' @description FoldersApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ ArchiveFolder } \emph{ Archive a folder }
#' Archives a folder to a lower storage cost tier.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } body \link{FolderArchiveRequest}
#' \item \emph{ @returnType } \link{FolderResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 202 | Accepted.
#'
#' \item return type : FolderResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Server Error
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BulkFolderUpdate } \emph{ Updates list of folders with metadata }
#' Updates list of folders with metadata
#'
#' \itemize{
#' \item \emph{ @param } body \link{BulkFolderUpdateRequest}
#' \item \emph{ @returnType } \link{BulkFolderUpdateResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Success
#'
#' \item return type : BulkFolderUpdateResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 202 | Accepted.
#'
#' \item return type : BulkFolderUpdateResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BulkMetadataFolderUpdate } \emph{ Updates list of folders with metadata }
#' Updates list of folders with metadata
#'
#' \itemize{
#' \item \emph{ @param } body \link{BulkFolderMetadataUpdateRequest}
#' \item \emph{ @returnType } \link{JobResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Success.
#'
#' \item return type : JobResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 202 | Accepted
#'
#' \item return type : JobResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CompleteFolderSession } \emph{ Complete a folder upload in GDS }
#' Complete a folder upload in GDS.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } session_id character
#' \item \emph{ @param } body \link{CompleteSessionRequest}
#' \item \emph{ @returnType } \link{SessionResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Completed upload session.
#'
#' \item return type : SessionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 202 | Upload session in progress.
#'
#' \item return type : SessionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CopyFolder } \emph{ Copy a folder }
#' Copy a folder into a target parent folder
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } body \link{FolderCopyRequest}
#' \item \emph{ @param } tenant_id character
#' \item \emph{ @returnType } \link{JobResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 202 | Accepted.
#'
#' \item return type : JobResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Server Error
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateFolder } \emph{ Create a folder in GDS and receive credentials for upload }
#' Create a folder entry in GDS. Returns temporary credentials for folder upload directly to S3 when the include=objectStoreAccess parameter is used. Volume ID or volume name is required for folder creation. If a folder path is provided and does not exist, GDS automatically creates the folder path in the appropriate account.
#'
#' \itemize{
#' \item \emph{ @param } body \link{CreateFolderRequest}
#' \item \emph{ @param } include character
#' \item \emph{ @returnType } \link{FolderWriteableResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 201 | Created new Folder.
#'
#' \item return type : FolderWriteableResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | A conflict was found. Make sure the new Folder doesn't already exist.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateFolderSession } \emph{ Create a session }
#' Create a session and credentials used for accessing the object store directly
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } body \link{CreateSessionRequest}
#' \item \emph{ @returnType } \link{CreateSessionResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 201 | Created
#'
#' \item return type : CreateSessionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad Request
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Not Found
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Error
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteFolder } \emph{ Deletes a folder by id }
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @returnType } \link{FolderResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 202 | Accepted.
#'
#' \item return type : FolderResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFolder } \emph{ Get information about a folder in GDS. }
#' Get information for the specified folder ID.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } tenant_id character
#' \item \emph{ @param } include_volume_metadata character
#' \item \emph{ @param } include_active_jobs character
#' \item \emph{ @param } metadata_include character
#' \item \emph{ @param } metadata_exclude character
#' \item \emph{ @param } include character
#' \item \emph{ @returnType } \link{FolderResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Success.
#'
#' \item return type : FolderResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFolderJob } \emph{ Get status of a folder job in GDS }
#' Get status of a folder job in GDS.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } job_id character
#' \item \emph{ @returnType } \link{JobResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Returned job.
#'
#' \item return type : JobResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFolderSession } \emph{ Get status of a folder upload in GDS }
#' Get status of a folder upload in GDS.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } session_id character
#' \item \emph{ @returnType } \link{SessionResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Completed upload session.
#'
#' \item return type : SessionResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListFolders } \emph{ Get a list of folders }
#' Given a volumeId or volume name, get a list of folders accessible by the JWT. The default sort returned is alphabetical, ascending. The default page size is 10 items
#'
#' \itemize{
#' \item \emph{ @param } volume_id list( character )
#' \item \emph{ @param } volume_name list( character )
#' \item \emph{ @param } path list( character )
#' \item \emph{ @param } job_statuses character
#' \item \emph{ @param } acls list( character )
#' \item \emph{ @param } recursive character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @param } page_token character
#' \item \emph{ @param } include character
#' \item \emph{ @param } tenant_id character
#' \item \emph{ @param } metadata_include character
#' \item \emph{ @param } metadata_exclude character
#' \item \emph{ @returnType } \link{FolderListResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Success.
#'
#' \item return type : FolderListResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UnarchiveFolder } \emph{ Unarchive a folder }
#' Unarchive a folder from a lower storage cost tier.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } body \link{FolderUnarchiveRequest}
#' \item \emph{ @returnType } \link{FolderResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 202 | Accepted.
#'
#' \item return type : FolderResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Conflict.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Server Error
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdateFolder } \emph{ Update a folder content or acl }
#' Update an existing folder in GDS and return upload credentials for that folder. Changes to the folder name and other metadata are not supported at this time.  Optionally overwrite the acl for this folder if acl is provided in the request.
#'
#' \itemize{
#' \item \emph{ @param } folder_id character
#' \item \emph{ @param } include character
#' \item \emph{ @param } body \link{FolderUpdateRequest}
#' \item \emph{ @returnType } \link{FolderWriteableResponse} \cr
#'
#' \item On encountering errors, an error of subclass ApiException will be thrown.
#'
#' \item status code : 200 | Success.
#'
#' \item return type : FolderWriteableResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Forbidden.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Folder not found.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected issue. Please try your request again. If problem persists, please contact the system administrator.
#'
#' \item return type : ErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  ArchiveFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to be archived.
#' var_body <- FolderArchiveRequest$new("storageTier_example") # FolderArchiveRequest | 
#'
#' #Archive a folder
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  BulkFolderUpdate  ####################
#'
#' library(icar1)
#' var_body <- BulkFolderUpdateRequest$new(c(BulkFolderUpdateItem$new("id_example", 123)), 123) # BulkFolderUpdateRequest |  (Optional)
#'
#' #Updates list of folders with metadata
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  BulkMetadataFolderUpdate  ####################
#'
#' library(icar1)
#' var_body <- BulkFolderMetadataUpdateRequest$new("folderId_example", "fileStatus_example", MetadataUpdateRequest$new(123, 123), MetadataUpdateRequest$new(123, 123), MetadataUpdateRequest$new(123, 123)) # BulkFolderMetadataUpdateRequest |  (Optional)
#'
#' #Updates list of folders with metadata
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  CompleteFolderSession  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder related to the upload session.
#' var_session_id <- "session_id_example" # character | The id of the upload session
#' var_body <- CompleteSessionRequest$new(123) # CompleteSessionRequest | The request body
#'
#' #Complete a folder upload in GDS
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  CopyFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to be copied.
#' var_body <- FolderCopyRequest$new("targetParentFolderId_example", "destinationFolderName_example", c("metadataToCopy_example"), "duplicateFileAction_example") # FolderCopyRequest | 
#' var_tenant_id <- "tenant_id_example" # character | Optional parameter to copy from a shared folder in another tenant (Optional)
#'
#' #Copy a folder
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  CreateFolder  ####################
#'
#' library(icar1)
#' var_body <- CreateFolderRequest$new("name_example", "folderPath_example", "volumeId_example", "volumeName_example", 123, c("acl_example")) # CreateFolderRequest | 
#' var_include <- "include_example" # character | Optionally include additional fields in the response.              Possible values: ObjectStoreAccess (Optional)
#'
#' #Create a folder in GDS and receive credentials for upload
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  CreateFolderSession  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | 
#' var_body <- CreateSessionRequest$new(123) # CreateSessionRequest | 
#'
#' #Create a session
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  DeleteFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to be deleted.
#'
#' #Deletes a folder by id
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  GetFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to retrieve.
#' var_tenant_id <- "tenant_id_example" # character | Optional parameter to see shared data in another tenant (Optional)
#' var_include_volume_metadata <- "include_volume_metadata_example" # character | Optional parameter to return volume's metadata (Optional)
#' var_include_active_jobs <- "include_active_jobs_example" # character | Optional parameter to return active jobs associated to folder (Optional)
#' var_metadata_include <- "metadata_include_example" # character | Optional parameter to specify comma separated patterns to include metadata by their field names. (Optional)
#' var_metadata_exclude <- "metadata_exclude_example" # character | Optional parameter to specify comma separated patterns to exclude metadata by their field names. (Optional)
#' var_include <- "include_example" # character | Optionally include additional fields in the response.              Possible values: ObjectStoreAccess (Optional)
#'
#' #Get information about a folder in GDS.
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  GetFolderJob  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder related to the job.
#' var_job_id <- "job_id_example" # character | The id of the job
#'
#' #Get status of a folder job in GDS
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  GetFolderSession  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder related to the upload session.
#' var_session_id <- "session_id_example" # character | The id of the upload session
#'
#' #Get status of a folder upload in GDS
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  ListFolders  ####################
#'
#' library(icar1)
#' var_volume_id <- c("inner_example") # array[character] | Optional field that specifies comma-separated volume IDs to include in the list (Optional)
#' var_volume_name <- c("inner_example") # array[character] | Optional field that specifies comma-separated volume names to include in the list (Optional)
#' var_path <- c("inner_example") # array[character] | Optional field that specifies comma-separated paths to include in the list. Value can use wildcards (e.g. /a/b/c/*) or exact matches (e.g. /a/b/c/d/). (Optional)
#' var_job_statuses <- "job_statuses_example" # character | Optional field that specifies comma-separated JobStatuses to include in the list (Optional)
#' var_acls <- c("inner_example") # array[character] | Optional field that specifies comma-separated acls to include in the list (Optional)
#' var_recursive <- "recursive_example" # character | Optional field to specify if folders should be returned recursively in and under the specified paths, or only directly in the specified paths (Optional)
#' var_page_size <- 56 # integer | START_DESC END_DESC (Optional)
#' var_page_token <- "page_token_example" # character | START_DESC END_DESC (Optional)
#' var_include <- "include_example" # character | Optionally include additional fields in the response. Multiple fields can be included by comma-separation.  Possible values: TotalItemCount, InheritedAcl (Optional)
#' var_tenant_id <- "tenant_id_example" # character | Optional parameter to see shared data in another tenant (Optional)
#' var_metadata_include <- "metadata_include_example" # character | Optional parameter to specify comma separated patterns to include metadata by their field names. (Optional)
#' var_metadata_exclude <- "metadata_exclude_example" # character | Optional parameter to specify comma separated patterns to exclude metadata by their field names. (Optional)
#'
#' #Get a list of folders
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  UnarchiveFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to be unarchived.
#' var_body <- FolderUnarchiveRequest$new("restoreSpeed_example") # FolderUnarchiveRequest | 
#'
#' #Unarchive a folder
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' ####################  UpdateFolder  ####################
#'
#' library(icar1)
#' var_folder_id <- "folder_id_example" # character | Unique identifier for the folder to be updated.
#' var_include <- "include_example" # character | Optionally include additional fields in the response.              Possible values: ObjectStoreAccess (Optional)
#' var_body <- FolderUpdateRequest$new(123, c("acl_example")) # FolderUpdateRequest |  (Optional)
#'
#' #Update a folder content or acl
#' api_instance <- FoldersApi$new()
#'
#' # Configure API key authorization: Bearer
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @importFrom rlang abort
#' @export
FoldersApi <- R6::R6Class(
  "FoldersApi",
  public = list(
    api_client = NULL,
    #' Initialize a new FoldersApi.
    #'
    #' @description
    #' Initialize a new FoldersApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Archive a folder
    #'
    #' @description
    #' Archive a folder
    #'
    #' @param folder_id Unique identifier for the folder to be archived.
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderResponse
    #' @export
    ArchiveFolder = function(folder_id, body, data_file = NULL, ...) {
      local_var_response <- self$ArchiveFolderWithHttpInfo(folder_id, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Archive a folder
    #'
    #' @description
    #' Archive a folder
    #'
    #' @param folder_id Unique identifier for the folder to be archived.
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderResponse) with additional information such as HTTP status code, headers
    #' @export
    ArchiveFolderWithHttpInfo = function(folder_id, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }



      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}:archive"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json-patch+json", "application/json", "text/json", "application/*+json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Updates list of folders with metadata
    #'
    #' @description
    #' Updates list of folders with metadata
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BulkFolderUpdateResponse
    #' @export
    BulkFolderUpdate = function(body = NULL, data_file = NULL, ...) {
      local_var_response <- self$BulkFolderUpdateWithHttpInfo(body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Updates list of folders with metadata
    #'
    #' @description
    #' Updates list of folders with metadata
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BulkFolderUpdateResponse) with additional information such as HTTP status code, headers
    #' @export
    BulkFolderUpdateWithHttpInfo = function(body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "BulkFolderUpdateResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Updates list of folders with metadata
    #'
    #' @description
    #' Updates list of folders with metadata
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobResponse
    #' @export
    BulkMetadataFolderUpdate = function(body = NULL, data_file = NULL, ...) {
      local_var_response <- self$BulkMetadataFolderUpdateWithHttpInfo(body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Updates list of folders with metadata
    #'
    #' @description
    #' Updates list of folders with metadata
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobResponse) with additional information such as HTTP status code, headers
    #' @export
    BulkMetadataFolderUpdateWithHttpInfo = function(body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/bulkMetadataFolderUpdate"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Complete a folder upload in GDS
    #'
    #' @description
    #' Complete a folder upload in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the upload session.
    #' @param session_id The id of the upload session
    #' @param body The request body
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SessionResponse
    #' @export
    CompleteFolderSession = function(folder_id, session_id, body, data_file = NULL, ...) {
      local_var_response <- self$CompleteFolderSessionWithHttpInfo(folder_id, session_id, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Complete a folder upload in GDS
    #'
    #' @description
    #' Complete a folder upload in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the upload session.
    #' @param session_id The id of the upload session
    #' @param body The request body
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SessionResponse) with additional information such as HTTP status code, headers
    #' @export
    CompleteFolderSessionWithHttpInfo = function(folder_id, session_id, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`session_id`)) {
        rlang::abort(message = "Missing required parameter `session_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `session_id`."))
      }

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }




      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}/sessions/{sessionId}:complete"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`session_id`)) {
        local_var_url_path <- gsub("\\{sessionId\\}", URLencode(as.character(`session_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "SessionResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Copy a folder
    #'
    #' @description
    #' Copy a folder
    #'
    #' @param folder_id Unique identifier for the folder to be copied.
    #' @param body 
    #' @param tenant_id (optional) Optional parameter to copy from a shared folder in another tenant
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobResponse
    #' @export
    CopyFolder = function(folder_id, body, tenant_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CopyFolderWithHttpInfo(folder_id, body, tenant_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Copy a folder
    #'
    #' @description
    #' Copy a folder
    #'
    #' @param folder_id Unique identifier for the folder to be copied.
    #' @param body 
    #' @param tenant_id (optional) Optional parameter to copy from a shared folder in another tenant
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobResponse) with additional information such as HTTP status code, headers
    #' @export
    CopyFolderWithHttpInfo = function(folder_id, body, tenant_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }




      query_params[["tenantId"]] <- `tenant_id`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}:copy"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json-patch+json", "application/json", "text/json", "application/*+json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a folder in GDS and receive credentials for upload
    #'
    #' @description
    #' Create a folder in GDS and receive credentials for upload
    #'
    #' @param body 
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderWriteableResponse
    #' @export
    CreateFolder = function(body, include = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateFolderWithHttpInfo(body, include, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a folder in GDS and receive credentials for upload
    #'
    #' @description
    #' Create a folder in GDS and receive credentials for upload
    #'
    #' @param body 
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderWriteableResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateFolderWithHttpInfo = function(body, include = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }



      query_params[["include"]] <- `include`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderWriteableResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create a session
    #'
    #' @description
    #' Create a session
    #'
    #' @param folder_id 
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CreateSessionResponse
    #' @export
    CreateFolderSession = function(folder_id, body, data_file = NULL, ...) {
      local_var_response <- self$CreateFolderSessionWithHttpInfo(folder_id, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create a session
    #'
    #' @description
    #' Create a session
    #'
    #' @param folder_id 
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CreateSessionResponse) with additional information such as HTTP status code, headers
    #' @export
    CreateFolderSessionWithHttpInfo = function(folder_id, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }



      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}/sessions"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "CreateSessionResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Deletes a folder by id
    #'
    #' @description
    #' Deletes a folder by id
    #'
    #' @param folder_id Unique identifier for the folder to be deleted.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderResponse
    #' @export
    DeleteFolder = function(folder_id, data_file = NULL, ...) {
      local_var_response <- self$DeleteFolderWithHttpInfo(folder_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Deletes a folder by id
    #'
    #' @description
    #' Deletes a folder by id
    #'
    #' @param folder_id Unique identifier for the folder to be deleted.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteFolderWithHttpInfo = function(folder_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }


      local_var_url_path <- "/v1/folders/{folderId}"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get information about a folder in GDS.
    #'
    #' @description
    #' Get information about a folder in GDS.
    #'
    #' @param folder_id Unique identifier for the folder to retrieve.
    #' @param tenant_id (optional) Optional parameter to see shared data in another tenant
    #' @param include_volume_metadata (optional) Optional parameter to return volume's metadata
    #' @param include_active_jobs (optional) Optional parameter to return active jobs associated to folder
    #' @param metadata_include (optional) Optional parameter to specify comma separated patterns to include metadata by their field names.
    #' @param metadata_exclude (optional) Optional parameter to specify comma separated patterns to exclude metadata by their field names.
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderResponse
    #' @export
    GetFolder = function(folder_id, tenant_id = NULL, include_volume_metadata = NULL, include_active_jobs = NULL, metadata_include = NULL, metadata_exclude = NULL, include = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetFolderWithHttpInfo(folder_id, tenant_id, include_volume_metadata, include_active_jobs, metadata_include, metadata_exclude, include, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get information about a folder in GDS.
    #'
    #' @description
    #' Get information about a folder in GDS.
    #'
    #' @param folder_id Unique identifier for the folder to retrieve.
    #' @param tenant_id (optional) Optional parameter to see shared data in another tenant
    #' @param include_volume_metadata (optional) Optional parameter to return volume's metadata
    #' @param include_active_jobs (optional) Optional parameter to return active jobs associated to folder
    #' @param metadata_include (optional) Optional parameter to specify comma separated patterns to include metadata by their field names.
    #' @param metadata_exclude (optional) Optional parameter to specify comma separated patterns to exclude metadata by their field names.
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderResponse) with additional information such as HTTP status code, headers
    #' @export
    GetFolderWithHttpInfo = function(folder_id, tenant_id = NULL, include_volume_metadata = NULL, include_active_jobs = NULL, metadata_include = NULL, metadata_exclude = NULL, include = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }








      query_params[["tenantId"]] <- `tenant_id`

      query_params[["includeVolumeMetadata"]] <- `include_volume_metadata`

      query_params[["includeActiveJobs"]] <- `include_active_jobs`

      query_params[["metadata.include"]] <- `metadata_include`

      query_params[["metadata.exclude"]] <- `metadata_exclude`

      query_params[["include"]] <- `include`

      local_var_url_path <- "/v1/folders/{folderId}"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get status of a folder job in GDS
    #'
    #' @description
    #' Get status of a folder job in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the job.
    #' @param job_id The id of the job
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobResponse
    #' @export
    GetFolderJob = function(folder_id, job_id, data_file = NULL, ...) {
      local_var_response <- self$GetFolderJobWithHttpInfo(folder_id, job_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get status of a folder job in GDS
    #'
    #' @description
    #' Get status of a folder job in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the job.
    #' @param job_id The id of the job
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobResponse) with additional information such as HTTP status code, headers
    #' @export
    GetFolderJobWithHttpInfo = function(folder_id, job_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`job_id`)) {
        rlang::abort(message = "Missing required parameter `job_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `job_id`."))
      }



      local_var_url_path <- "/v1/folders/{folderId}/jobs/{jobId}"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{jobId\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get status of a folder upload in GDS
    #'
    #' @description
    #' Get status of a folder upload in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the upload session.
    #' @param session_id The id of the upload session
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SessionResponse
    #' @export
    GetFolderSession = function(folder_id, session_id, data_file = NULL, ...) {
      local_var_response <- self$GetFolderSessionWithHttpInfo(folder_id, session_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get status of a folder upload in GDS
    #'
    #' @description
    #' Get status of a folder upload in GDS
    #'
    #' @param folder_id Unique identifier for the folder related to the upload session.
    #' @param session_id The id of the upload session
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SessionResponse) with additional information such as HTTP status code, headers
    #' @export
    GetFolderSessionWithHttpInfo = function(folder_id, session_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`session_id`)) {
        rlang::abort(message = "Missing required parameter `session_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `session_id`."))
      }



      local_var_url_path <- "/v1/folders/{folderId}/sessions/{sessionId}"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`session_id`)) {
        local_var_url_path <- gsub("\\{sessionId\\}", URLencode(as.character(`session_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "SessionResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get a list of folders
    #'
    #' @description
    #' Get a list of folders
    #'
    #' @param volume_id (optional) Optional field that specifies comma-separated volume IDs to include in the list
    #' @param volume_name (optional) Optional field that specifies comma-separated volume names to include in the list
    #' @param path (optional) Optional field that specifies comma-separated paths to include in the list. Value can use wildcards (e.g. /a/b/c/*) or exact matches (e.g. /a/b/c/d/).
    #' @param job_statuses (optional) Optional field that specifies comma-separated JobStatuses to include in the list
    #' @param acls (optional) Optional field that specifies comma-separated acls to include in the list
    #' @param recursive (optional) Optional field to specify if folders should be returned recursively in and under the specified paths, or only directly in the specified paths
    #' @param page_size (optional) START_DESC END_DESC
    #' @param page_token (optional) START_DESC END_DESC
    #' @param include (optional) Optionally include additional fields in the response. Multiple fields can be included by comma-separation.  Possible values: TotalItemCount, InheritedAcl
    #' @param tenant_id (optional) Optional parameter to see shared data in another tenant
    #' @param metadata_include (optional) Optional parameter to specify comma separated patterns to include metadata by their field names.
    #' @param metadata_exclude (optional) Optional parameter to specify comma separated patterns to exclude metadata by their field names.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderListResponse
    #' @export
    ListFolders = function(volume_id = NULL, volume_name = NULL, path = NULL, job_statuses = NULL, acls = NULL, recursive = NULL, page_size = NULL, page_token = NULL, include = NULL, tenant_id = NULL, metadata_include = NULL, metadata_exclude = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListFoldersWithHttpInfo(volume_id, volume_name, path, job_statuses, acls, recursive, page_size, page_token, include, tenant_id, metadata_include, metadata_exclude, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get a list of folders
    #'
    #' @description
    #' Get a list of folders
    #'
    #' @param volume_id (optional) Optional field that specifies comma-separated volume IDs to include in the list
    #' @param volume_name (optional) Optional field that specifies comma-separated volume names to include in the list
    #' @param path (optional) Optional field that specifies comma-separated paths to include in the list. Value can use wildcards (e.g. /a/b/c/*) or exact matches (e.g. /a/b/c/d/).
    #' @param job_statuses (optional) Optional field that specifies comma-separated JobStatuses to include in the list
    #' @param acls (optional) Optional field that specifies comma-separated acls to include in the list
    #' @param recursive (optional) Optional field to specify if folders should be returned recursively in and under the specified paths, or only directly in the specified paths
    #' @param page_size (optional) START_DESC END_DESC
    #' @param page_token (optional) START_DESC END_DESC
    #' @param include (optional) Optionally include additional fields in the response. Multiple fields can be included by comma-separation.  Possible values: TotalItemCount, InheritedAcl
    #' @param tenant_id (optional) Optional parameter to see shared data in another tenant
    #' @param metadata_include (optional) Optional parameter to specify comma separated patterns to include metadata by their field names.
    #' @param metadata_exclude (optional) Optional parameter to specify comma separated patterns to exclude metadata by their field names.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderListResponse) with additional information such as HTTP status code, headers
    #' @export
    ListFoldersWithHttpInfo = function(volume_id = NULL, volume_name = NULL, path = NULL, job_statuses = NULL, acls = NULL, recursive = NULL, page_size = NULL, page_token = NULL, include = NULL, tenant_id = NULL, metadata_include = NULL, metadata_exclude = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      if (`page_size` > 10000) {
        rlang::abort(message = "Invalid value for `page_size` when calling FoldersApi$ListFolders, must be smaller than or equal to 10000.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Invalid value for `page_size` when calling FoldersApi$ListFolders, must be smaller than or equal to 10000."))
      }
      if (`page_size` < 0) {
        rlang::abort(message = "Invalid value for `page_size` when calling FoldersApi$ListFolders, must be bigger than or equal to 0.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Invalid value for `page_size` when calling FoldersApi$ListFolders, must be bigger than or equal to 0."))
      }






      # no explore
      query_params[["volume.id"]] <- I(paste(lapply(`volume_id`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["volume.name"]] <- I(paste(lapply(`volume_name`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["path"]] <- I(paste(lapply(`path`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["jobStatuses"]] <- `job_statuses`

      # no explore
      query_params[["acls"]] <- I(paste(lapply(`acls`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["recursive"]] <- `recursive`

      query_params[["pageSize"]] <- `page_size`

      query_params[["pageToken"]] <- `page_token`

      query_params[["include"]] <- `include`

      query_params[["tenantId"]] <- `tenant_id`

      query_params[["metadata.include"]] <- `metadata_include`

      query_params[["metadata.exclude"]] <- `metadata_exclude`

      local_var_url_path <- "/v1/folders"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderListResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Unarchive a folder
    #'
    #' @description
    #' Unarchive a folder
    #'
    #' @param folder_id Unique identifier for the folder to be unarchived.
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderResponse
    #' @export
    UnarchiveFolder = function(folder_id, body, data_file = NULL, ...) {
      local_var_response <- self$UnarchiveFolderWithHttpInfo(folder_id, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Unarchive a folder
    #'
    #' @description
    #' Unarchive a folder
    #'
    #' @param folder_id Unique identifier for the folder to be unarchived.
    #' @param body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderResponse) with additional information such as HTTP status code, headers
    #' @export
    UnarchiveFolderWithHttpInfo = function(folder_id, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }

      if (missing(`body`)) {
        rlang::abort(message = "Missing required parameter `body`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `body`."))
      }



      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}:unarchive"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json-patch+json", "application/json", "text/json", "application/*+json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Update a folder content or acl
    #'
    #' @description
    #' Update a folder content or acl
    #'
    #' @param folder_id Unique identifier for the folder to be updated.
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FolderWriteableResponse
    #' @export
    UpdateFolder = function(folder_id, include = NULL, body = NULL, data_file = NULL, ...) {
      local_var_response <- self$UpdateFolderWithHttpInfo(folder_id, include, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Update a folder content or acl
    #'
    #' @description
    #' Update a folder content or acl
    #'
    #' @param folder_id Unique identifier for the folder to be updated.
    #' @param include (optional) Optionally include additional fields in the response.              Possible values: ObjectStoreAccess
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FolderWriteableResponse) with additional information such as HTTP status code, headers
    #' @export
    UpdateFolderWithHttpInfo = function(folder_id, include = NULL, body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`folder_id`)) {
        rlang::abort(message = "Missing required parameter `folder_id`.",
                     .subclass = "ApiException",
                     ApiException = ApiException$new(status = 0,
                                                     reason = "Missing required parameter `folder_id`."))
      }




      query_params[["include"]] <- `include`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/folders/{folderId}"
      if (!missing(`folder_id`)) {
        local_var_url_path <- gsub("\\{folderId\\}", URLencode(as.character(`folder_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json-patch+json", "application/json", "text/json", "application/*+json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FolderWriteableResponse", loadNamespace("icar1")),
          error = function(e) {
            rlang::abort(message = "Failed to deserialize response",
                         .subclass = "ApiException",
                         ApiException = ApiException$new(http_response = local_var_resp))
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
